apply plugin: 'java'
repositories { jcenter() }
dependencies {
  runtime 'com.netflix.exhibitor:exhibitor-standalone:<%= node["exhibitor"]["version"] %>'
}
task copyLibs(type: Copy) {
  into './libs'
  from configurations.runtime
}
